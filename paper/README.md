# PO3 Pipeline Paper

This directory contains a conference-style LaTeX paper describing the PO3 pipelined out-of-order CPU model.

- `po3_pipeline.tex`: main paper source.
- `Makefile`: optional local build helper using `pdflatex`.

Build from this directory with:

```bash
make
```
